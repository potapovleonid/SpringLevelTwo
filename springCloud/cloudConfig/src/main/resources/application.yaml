server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bolshakov-coach/springcloud-config.git
          search-paths: springleveltwo/lesson05/step04
          clone-on-start: true
  application:
    name: config_server

logging:
  level:
    org.springframework.cloud: DEBUG